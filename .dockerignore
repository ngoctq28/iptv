# Version control
.git
.gitignore

# Node deps (re-installed inside the container)
node_modules

# Docker files themselves
Dockerfile
.dockerignore
docker-compose.yml

# Build output (rebuilt inside the container)
dist

# Cloudflare Workers / Pages (not used in Docker)
functions
wrangler.toml

# Docs / misc
README.md
*.md

# OS / editor artefacts
.DS_Store
Thumbs.db
.env*
